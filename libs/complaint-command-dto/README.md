# complaint-command-dto

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build complaint-command-dto` to build the library.

## Running unit tests

Run `nx test complaint-command-dto` to execute the unit tests via [Jest](https://jestjs.io).
